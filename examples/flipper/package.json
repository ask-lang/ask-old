{
  "name": "flipper",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npx asc ./index.ts --importMemory --initialMemory 2 --maximumMemory 16 --noExportMemory --runtime none --use abort= -O3z -b main.wasm -t main.wast"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@assemblyscript/loader": "^0.17",
    "assemblyscript": "^0.17"
  }
}
